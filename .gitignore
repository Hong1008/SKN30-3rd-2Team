# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env
.env.*
!.env.example
.tools

# IDE
.vscode/
.antigravitycli/
.idea/
.DS_Store

# 생성물: `just build-db` 로 재생성되는 DB 파생물 (normalize JSON/SQL이 진실의 원천)
# 바이너리는 git 으로 관리하지 않습니다. (data/README.md 참고)
data/migration/*.sqlite3
data/migration/*.sqlite3-shm
data/migration/*.sqlite3-wal
# Chroma 벡터 인덱스 (RDB와 생명주기가 달라 별도 폴더에 격리, build_index.py 가 통째로 재생성)
data/chroma/

# 사용자 업로드 임시 파일
data/99_uploads/

# 로컬 임베딩/리랭커 모델 및 ONNX/양자화 바이너리 (대용량 파일 제외)
*onnx*/
*onnx*.zip
# eval 트랙 B: kordoc 변환 산출물(재생성물)
quality/fixtures/track_b/golden_b/converted/
